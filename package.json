{
  "name": "driving-date-search",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for a UK driving test cancellation finder (mobile + backend + scraper)",
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "backend": "npm --workspace backend run dev",
    "mobile": "npm --workspace mobile run start",
    "lint:backend": "npm --workspace backend run lint"
  },
  "license": "MIT"
}
