# Base URL of the backend API. On a physical device use your machine's LAN IP,
# e.g. http://192.168.1.20:4000 (localhost won't resolve from the phone).
# This is read via app.json "extra.apiUrl"; update it there for builds.
EXPO_PUBLIC_API_URL=http://localhost:4000
